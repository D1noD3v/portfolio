import "@/public/style.css";
import "@/public/terminal.png";
import "@/public/fonts/KronaOne-Regular.ttf"

export default function App({ Component, pageProps }) {
    return <Component {...pageProps} />
}